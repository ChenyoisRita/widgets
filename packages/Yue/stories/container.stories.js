import React from 'react';
import { SimpleContainer } from '../src/container';

export default {
    component: SimpleContainer,
    title: 'Container',
  };

export const Default = () => <SimpleContainer />;
